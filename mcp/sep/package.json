{
  "name": "sep-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Self-contained SEP (Stanford Encyclopedia of Philosophy) MCP server for the PHILOSOPHY · ORACLE plugin. Ships its own dataset (1,719 entries / 31,001 edges / embeddings / 867 ATLAS topics) — no external database or API key. Only semantic search needs transformers; every other tool runs with zero dependencies.",
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
